# Build Outputs

This directory contains generated publish-planning artifacts for the knowledge hub MVP.

Current expected outputs:

- `manifests/`
- `sites/`

The planning step generates audience-specific manifests and staged content trees that Quartz can consume later.
