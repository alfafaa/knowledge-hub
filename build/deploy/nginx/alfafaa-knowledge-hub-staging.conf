# Generated for Alfafaa Knowledge Hub
# This file is generated. Do not edit manually.

server {
    listen 8088;
    listen [::]:8088;
    server_name _;

    root /srv/alfafaa-knowledge-hub/internal-site/current;
    index index.html;

    access_log /var/log/nginx/internal-site.access.log;
    error_log /var/log/nginx/internal-site.error.log;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location ~* \.(css|js|png|jpg|jpeg|gif|svg|ico|webp|xml|json)$ {
        expires 1h;
        add_header Cache-Control "public";
        try_files $uri =404;
    }
}

server {
    listen 8089;
    listen [::]:8089;
    server_name _;

    root /srv/alfafaa-knowledge-hub/engineering-site/current;
    index index.html;

    access_log /var/log/nginx/engineering-site.access.log;
    error_log /var/log/nginx/engineering-site.error.log;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location ~* \.(css|js|png|jpg|jpeg|gif|svg|ico|webp|xml|json)$ {
        expires 1h;
        add_header Cache-Control "public";
        try_files $uri =404;
    }
}
