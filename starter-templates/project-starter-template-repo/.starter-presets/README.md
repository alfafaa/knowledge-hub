# Starter Presets

This area contains starter presets that help teams adapt the repository after extracting the template ZIP.

Current presets:

- `docs-config/`

Use these presets to choose the correct `docs/docs.config.yaml` starting point for the real repo type.
