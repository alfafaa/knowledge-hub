# Schemas

This directory contains machine-readable schema definitions used by the MVP validator and sync planner.

Current files:

- `frontmatter-schema.yaml`
- `docs-config-schema.yaml`

These schemas are intentionally lightweight and tuned for the current implementation phase.
